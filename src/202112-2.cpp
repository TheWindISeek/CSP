#include <cstdio>
#include <cstdlib>
#include <cmath>

using namespace std;

int a[100003];

int main () {
	int n, N;
	scanf("%d %d", &n, &N);
	for(int i = 1; i <= n; ++i) {
		scanf("%d", &a[i]);
	}
	int error = 0;
	
	return 0;
}